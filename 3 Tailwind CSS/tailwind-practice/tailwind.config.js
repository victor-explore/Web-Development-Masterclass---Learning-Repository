/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{html,js}"], // Specifies which files Tailwind should scan for classes
  theme: {
    extend: {}, // Allows extending Tailwind's default theme
  },
  plugins: [], // Additional Tailwind plugins can be added here
};
